//Code by G V V Sharma
//October 23, 2023
//matrix operations using lists
#include <stdio.h>
#include <stdlib.h>
#include "libs/listfun.h"


int  main()
{
FILE *fp; //file pointer
double val;//for reading file data
avyuh *head, *temp;//head of the array
sadish *a,*heads;
int m =2, n=3, i, j;
//fp = fopen("A.dat","r");
//heads = loadVec(fp,3);
//head = loadList("A.dat", 3, 1);
//a = Vecind(heads,0);
//printf("%lf\n",a->data);
//printList(head,3,1);
//head = loadList("vertices.dat", m, n);
//printList(head,m,n);
//fclose(fp);
    return 0;
}
